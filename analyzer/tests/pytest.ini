[pytest]
testpaths = .
markers =
    unit: Fast tests, no ML models, no real images needed
    integration: Needs ML model weights in analyzer/models/
    e2e: End-to-end pipeline, slow
    compat: Backwards compatibility migration tests
    ui: Launches the full application
