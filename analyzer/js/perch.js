    // Share-with-Perch dialog + progress card (Perch sync feature lives
    // on perch-dev-v2 — this file is a placeholder on dev so script load
    // order matches and js/*.js merges cleanly across branches).
