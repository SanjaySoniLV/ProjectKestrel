# Core ML/AI Dependencies
# torch, torchvision, tensorflow removed: all ML inference uses ONNX Runtime.
# onnxruntime on macOS includes CoreMLExecutionProvider for GPU/Neural Engine
# acceleration via Apple Core ML. No separate package needed (unlike DirectML on Windows).
onnxruntime==1.23.2

numpy==2.4.4

# HTTP and SSL dependencies
certifi

# Computer Vision
opencv-python==4.11.0.86
rawpy==0.27.0

# Data Processing
pandas==2.2.2

# Utilities
PyExifTool==0.5.6
requests==2.34.2
pywebview==6.2.1
keyring

# macOS App Sandbox helpers (security-scoped bookmarks, NSWorkspace) in
# mac_sandbox.py talk to Cocoa via PyObjC. pywebview's Cocoa backend already
# pulls these transitively; pinned explicitly so the App Store build can rely
# on Foundation/AppKit being present and PyInstaller collects them.
pyobjc-core==12.2.1
pyobjc-framework-Cocoa==12.2.1

# installer
pyinstaller==6.20.0

# EXIF reading
exifread
pillow==12.2.0

# speciesnet, segment-anything-hq, timm removed: ensemble logic is inlined in
# kestrel_analyzer/ml/_speciesnet_ensemble.py; SAM-HQ runs via ONNX Runtime.