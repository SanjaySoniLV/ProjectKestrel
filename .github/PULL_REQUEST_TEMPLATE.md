<!--
Target this PR at `dev`, not `main`. `main` only takes direct changes for urgent fixes that need
to be deployed immediately — everything else (features, refactors, routine bug fixes) goes through
`dev` first. See AGENTS.md for full contribution guidelines.
-->

## The problem

<!-- What's broken, missing, or awkward today, in plain terms. Assume the reader hasn't seen the issue before. -->

## How to reproduce

<!-- The concrete steps (or scenario) that show the problem, so a reviewer can confirm it existed before this fix. -->

## What this PR does

<!-- The actual change, and why this approach was chosen over alternatives if that isn't obvious from the diff. -->
